{
  "name": "legislative-juppe",
  "version": "1.0.0",
  "description": "TODO",
  "scripts": {
    "start": "http-server",
    "watch": "watchify js/main.js -o js/bundle.js -d -v"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": ["es2015"]
        }
      ]
    ]
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "http-server": "^0.10.0",
    "watchify": "^3.9.0"
  },
  "dependencies": {
    "nanomorph": "^5.1.3",
    "redux": "^3.7.2"
  }
}
